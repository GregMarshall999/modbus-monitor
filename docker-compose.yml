services:
  modbus-monitor:
    build: .
    image: modbus-monitor:latest
    container_name: modbus-monitor
    ports:
      - "8000:8000"
    devices:
      - "/dev/ttyUSB0:/dev/ttyUSB0"
    restart: unless-stopped
